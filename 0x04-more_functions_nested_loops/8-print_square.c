#include "main.h"

/**
 * print_square - print a square using #
 *
 * @size: size of square
 *
 * Return: Always 0 (success)
*/

void print_square(int size)
{
	int lenth, wide;

	for (lenth = 1; lenth <= size; lenth++)
	{
		for (wide = 1; wide <= size; colum++)
			_putchar('#');
		_putchar('\n');
	}
}
