#include <stdio.h>
/**
 * main - Enter point
 *
 * Discription: print single digit numbers of base 10
 *
 * Return: always 0 (success)
*/

int main(void)
{
	int n = 48;

	while (n < 58)
	{
	putchar(n);
	n++;
	}
	putchar('\n');

	return (0);
}
